"use strict"; // treat all JS code as newer version of JS

// alert(5+5); // we are using node.js, not browser

console.log(5
    +
    5 )  // code readability should be high

console.log("Tirth");

let num = 17;

// number => 2 to the power 53
// boolean => true/false
// string => " "
// null => standalone value  // temperature example: if server, due to some error, doesn't have the temperature value then it may send 0, but that is also a temperature value so null is used in this case
// bigint
// undefined => value hasn't been assigned yet
// symbol => unique

// above all are primitive datatype

// object

console.log(typeof "tirth");
console.log(typeof null);  // returns "object"
console.log(typeof undefined);  // returns "undefined"





